# Designing AI Agents — 项目手册

> 这份手册的目的：帮你搞清楚"每个文件是什么、为什么存在、和其他文件是什么关系"。  
> 配合书本章节阅读，不容易迷路。

---

## 一、整体一句话

这个项目是一本书（Manning《Designing AI Agents》）的配套代码。  
书里用一个叫 **Argus（代码审查 Agent）** 的例子贯穿始终，每章给它加一个新能力，同时讲清楚这个能力背后的设计模式。

---

## 二、两类文件，别搞混

每一章目录下都有两个子目录，职责完全不同：

```
ch0X-xxx/
├── patterns/   ← 【设计模式】每个文件一个独立概念，可单独学习和运行
└── argus/      ← 【Argus 快照】把本章学到的模式装进 Argus，看整体如何运转
```

| 目录 | 是什么 | 怎么用 |
|------|--------|--------|
| `patterns/` | 教具。每个文件演示一个设计模式，互不依赖 | 先看这里，理解概念 |
| `argus/` | 作业。把多个模式组合进真实 Agent | 看完 patterns 再看这里，理解整合 |

> **类比**：`patterns/` 是"单个武功招式"，`argus/` 是"把招式组合成套路"。

---

## 三、Argus 是什么

Argus = 一个做**代码审查**的 AI Agent，名字来自希腊神话的百眼巨人（象征全方位感知）。

它不是一次写完的，而是**每章加一个模块**，逐步进化：

```
ch02  →  ch03  →  ch04  →  ch05  →  ch06  →  ch07  →  ch08  →  ch09
 核心     感知     记忆     推理     行动     反思    协作     治理
 PRA      看懂     记住     想清     做到     改进    分工     守规
 循环     代码     历史     楚问     结果     错误    任务     矩
```

**最终形态（ch09/ch10）的 Argus 能做什么**：
1. 读取代码变更（diff）
2. 感知相关上下文（哪些文件和这次改动有关）
3. 查询历史记忆（这个项目之前有没有犯过类似的错）
4. 推理分析（Chain of Thought 思考过程）
5. 调用工具（运行 lint、搜索代码）
6. 反思自检（对自己的审查结果再做批判）
7. 并行协作（复杂 diff 拆给多个子 Agent）
8. 治理审计（每个动作都有权限检查和日志）

---

## 四、各章目录

### 📁 ch01-paradigm-shift — 第1章：范式转变

**目的**：用最少的代码说明"传统软件"和"Agent"的本质区别，建立直觉。

```
ch01-paradigm-shift/
└── simple_agent.py     ← 两段代码的对比，无需 API Key
```

**核心代码对比**：
```python
# 传统软件：所有分支预先写死
class CheckoutService:
    def process_order(self, order):
        if order.total > self.limit: return self.reject(order)
        if not self.inventory.has(order.items): return self.reject(order)
        return self.fulfill(order)

# Agent：给目标和工具，自己决定怎么做
class AgentCheckout:
    def process_order(self, order):
        context = self.perceive(order)
        while not self.done(context):
            action = self.reason(context)   # LLM 在这里决策
            result = self.act(action)
            context = self.update(context, result)
```

**学完之后应该理解**：Agent 的核心是"循环 + LLM 决策"，而不是硬编码的 if-else。

---

### 📁 ch02-architecture — 第2章：Agent 架构

**目的**：搭建 Agent 的骨架，理解 PRA 循环、基础设施层、安全护栏，并对比不同框架。

```
ch02-architecture/
├── argus/
│   ├── core.py                      ← Argus 最初形态，53行，PRA 一次循环
│   ├── coding_agent_simplified.py   ← 带 while 循环的完整 Agent（概念性）
│   └── runtime.py                   ← 基础设施层草图（概念性，不可独立运行）
├── patterns/
│   └── openai_guardrails.py         ← 护栏模式：输入/输出双重过滤
└── demos/
    ├── openai_argus.py              ← 用 OpenAI SDK 实现同一个 Argus
    └── langgraph_argus.py           ← 用 LangGraph 实现同一个 Argus
```

**文件关系**：各文件**互相独立**，不存在调用关系，每个演示一个切面：

| 文件 | 演示的问题 | 能独立运行？ |
|------|-----------|-------------|
| `core.py` | 最简单的 PRA 是什么样的？ | ✅ 需要 ANTHROPIC_API_KEY |
| `coding_agent_simplified.py` | while 循环怎么写？ | ❌ 概念伪代码 |
| `runtime.py` | 基础设施层包含哪些组件？ | ❌ 概念伪代码 |
| `openai_guardrails.py` | 护栏怎么配置？ | ❌ 占位符函数 |
| `demos/openai_argus.py` | OpenAI SDK 怎么写 Agent？ | ✅ 需要 OPENAI_API_KEY |
| `demos/langgraph_argus.py` | LangGraph 怎么写 Agent？ | ✅ 需要配置 LLM |

**核心概念 PRA 循环**：
```
感知(Perception) → 推理(Reasoning) → 行动(Action)
       ↑                                    |
       └──────────── 结果反馈 ──────────────┘
```

**Runtime VM 的五个组件**：

| 组件 | 作用 |
|------|------|
| Sandbox | 隔离执行，防止破坏系统 |
| StateManager | 持久化状态，崩溃可恢复 |
| MCPHost | 连接外部工具（MCP = Anthropic 的工具协议） |
| SkillRegistry | 管理可复用技能 |
| ObservabilityLayer | 日志、监控、可观测性 |

> 💡 **建议学习顺序**：core.py → coding_agent_simplified.py → runtime.py → openai_guardrails.py → demos/

---

### 📁 ch03-perception — 第3章：感知

**目的**：解决"Agent 怎么从海量信息中挑出对当前任务最有用的内容"。

```
ch03-perception/
├── argus/
│   └── perception.py               ← Argus 感知管道（将4个模式组合进 Argus）
└── patterns/
    ├── context_triage.py            ← 模式1：上下文分诊（优先级分配）
    ├── semantic_compaction.py       ← 模式2：语义压缩（压缩上下文，保留关键信息）
    ├── progressive_discovery.py     ← 模式3：渐进发现（从宽到窄的三阶段搜索）
    └── multimodal_fusion.py         ← 模式4：多模态融合（统一处理文本/代码/图片）
```

**4个感知模式是什么**：

| 模式 | 解决的问题 | 类比 |
|------|-----------|------|
| Context Triage（上下文分诊） | 信息太多，上下文窗口装不下 | 急诊分诊：优先处理重症 |
| Semantic Compaction（语义压缩） | 保留重要内容，压缩不重要的 | 新闻摘要，但不能丢关键事实 |
| Progressive Discovery（渐进发现） | 不知道要找什么，只能一步步缩小范围 | 侦探办案：先广撒网，再精准追踪 |
| Multimodal Fusion（多模态融合） | 输入可能是文字、代码、截图等混合 | 把各种格式统一翻译成 Agent 能理解的形式 |

> 💡 `context_triage.py` 不需要 API Key，可以直接运行看效果。

---

### 📁 ch04-memory — 第4章：记忆

**目的**：解决"Agent 怎么记住过去的经验，跨任务、跨会话保持知识积累"。

```
ch04-memory/
├── argus/
│   └── memory.py                   ← Argus 跨会话记忆模块
└── patterns/
    ├── hierarchical_memory.py       ← 模式1：三层记忆（工作/情节/语义）
    ├── rag_pipeline.py              ← 模式2：RAG 管道（向量检索增强）
    ├── progress_tracker.py          ← 模式3：进度追踪（崩溃可恢复的检查点）
    └── failure_journal.py           ← 模式4：失败日志（记录错误，避免重蹈覆辙）
```

**4个记忆模式是什么**：

| 模式 | 解决的问题 | 类比 |
|------|-----------|------|
| Hierarchical Memory（层级记忆） | 不同时间粒度的记忆怎么管理 | 人脑：短期记忆→长期记忆→技能记忆 |
| RAG Pipeline（检索增强） | 知识库太大，不能全放上下文 | 考试时查资料，而不是死记硬背 |
| Progress Tracker（进度追踪） | 任务中途崩溃了怎么办 | 游戏存档，死了从存档点复活 |
| Failure Journal（失败日志） | 同样的错误犯两次 | 飞行员事故报告：强制记录避免重复 |

> 💡 `progress_tracker.py` 不需要 API Key，可以直接运行。

---

### 📁 ch05-reasoning — 第5章：推理

**目的**：解决"Agent 怎么思考得更清晰、更深入、更准确"。

```
ch05-reasoning/
├── argus/
│   └── reasoning.py                ← Argus 推理方法片段（概念性，不可独立运行）
└── patterns/
    ├── reasoning_trace.py           ← 工具：推理过程可观测性（记录思考轨迹）
    ├── chain_of_thought.py          ← 模式1：思维链（分步推理 + 薄弱环节验证）
    ├── complexity_routing.py        ← 模式2：复杂度路由（简单问题用小模型，难题用大模型）
    ├── tree_of_thoughts.py          ← 模式3：思维树（多路径探索 + MCTS）
    └── hypothesis_testing.py        ← 模式4：假设检验（科学方法式推理）
```

**4个推理模式是什么**：

| 模式 | 解决的问题 | 类比 |
|------|-----------|------|
| Chain of Thought（思维链） | 直接回答容易出错 | 解数学题时写出每一步，而不是直接写答案 |
| Complexity Routing（复杂度路由） | 所有问题用大模型太贵、太慢 | 小病看诊所，大病才去三甲医院 |
| Tree of Thoughts（思维树） | 有些问题需要探索多条路径 | 下棋时考虑多个走法，选最好的 |
| Hypothesis Testing（假设检验） | 不确定时如何系统性地验证 | 科学家的实验方法：提假设→设计实验→验证→结论 |

---

### 📁 ch06-action — 第6章：行动

**目的**：解决"Agent 怎么把决策变成真实的工具调用和副作用，同时保证安全"。

```
ch06-action/
├── argus/
│   └── action.py                   ← Argus 行动层（累积快照：含 ch2-5 所有模块）
└── patterns/
    ├── action_trace.py              ← 工具：每次工具调用的可观测性记录
    ├── prompt_chain.py              ← 模式1：Prompt 链（多步骤流水线 + 质量门控）
    ├── tool_dispatch.py             ← 模式2：工具分发（能力总线 + 语义路由）
    ├── mcp_client.py                ← 工具：MCP 协议的最小客户端实现
    ├── plan_and_execute.py          ← 模式3：计划-执行（规划器和执行器分离）
    └── guardrail_sandwich.py        ← 模式4：护栏三明治（输入→执行→输出三层检查）
```

**4个行动模式是什么**：

| 模式 | 解决的问题 | 类比 |
|------|-----------|------|
| Prompt Chain（Prompt 链） | 复杂任务需要多步骤处理 | 工厂流水线，每站做一道工序 |
| Tool Dispatch（工具分发） | Agent 有很多工具，怎么找到正确的那个 | 总机接线员：根据需求转接到对应部门 |
| Plan and Execute（计划-执行） | 长任务需要先规划再执行，执行中要能调整计划 | 项目经理（规划）+ 工程师（执行）分开 |
| Guardrail Sandwich（护栏三明治） | 行动前后都需要安全检查 | 手术前检查患者身份，手术后核查器械数量 |

---

### 📁 ch07-reflection — 第7章：反思

**目的**：解决"Agent 怎么发现自己的错误并自我改进"。

```
ch07-reflection/
├── argus/
│   ├── reflection.py               ← Argus 反思模块
│   └── self_heal.py                ← Argus 有界重试与回滚
└── patterns/
    ├── reflection_trace.py          ← 工具：反思决策的可观测性记录
    ├── generator_critic.py          ← 模式1：生成-批评（一个生成，一个挑毛病）
    ├── self_heal_loop.py            ← 模式2：自愈循环（测试失败→自动修复→重试）
    ├── experience_replay.py         ← 模式3：经验回放（从历史运行中提取教训）
    └── skill_library.py             ← 模式4：技能库（把验证过的方法打包复用）
```

**4个反思模式是什么**：

| 模式 | 解决的问题 | 类比 |
|------|-----------|------|
| Generator-Critic（生成-批评） | 一个 Agent 既当运动员又当裁判，容易自我感觉良好 | 作家写完初稿，编辑来挑毛病，再修改 |
| Self-Heal Loop（自愈循环） | 代码修复失败了，不能无限重试 | 机器人跌倒自动站起来，但超过N次就停下来求助 |
| Experience Replay（经验回放） | 之前跑过的任务里有很多经验没有利用 | 运动员看回放录像，找自己的失误 |
| Skill Library（技能库） | 同一类问题每次都要从头想 | 老工匠把经验写成操作手册，后人直接查 |

---

### 📁 ch09-governance — 第9章：治理

**目的**：解决"Agent 在生产环境怎么保证安全、合规、可审计"。

```
ch09-governance/
└── patterns/
    ├── permission_gate.py           ← 权限门控（基于能力的访问控制）
    ├── policy_engine.py             ← 策略引擎（声明式规则评估）
    ├── trust_levels.py              ← 信任等级（动态调整 Agent 权限）
    ├── audit_log.py                 ← 审计日志（哈希链，防篡改）
    ├── governance_trace.py          ← 治理轨迹（记录每个治理决策）
    └── adversarial_review.py        ← 对抗性审查（红队测试）
    └── ... (还包含前几章的模式，累积)
```

**核心概念：信任等级 + 权限门控**

```
新 Agent（低信任）
    ↓ 成功完成任务 → 信任提升 → 获得更多能力
    ↓ 失败/违规   → 信任降低 → 能力受限

权限门控：
  PERCEIVE（读文件）→ 权限最低
  RUN_LINT（跑检查）→ 中等权限
  WRITE_FILES（写文件）→ 高权限
  NETWORK（网络请求）→ 最高权限，需人工审批
```

**审计日志的设计**（`audit_log.py`）：
- 每条记录包含：时间戳、操作者、动作、决策结果
- 用哈希链连接：每条记录包含上一条的哈希值
- 任何篡改都会导致哈希链断裂，可以被检测到

---

### 📁 ch10-methodology — 第10章：方法论

**目的**：汇总全书所有模式，给出完整的 Argus 最终形态和 28 个设计模式的参考实现。

```
ch10-methodology/
├── argus/                          ← Argus 完整最终形态（含所有章节模块）
│   ├── core.py
│   ├── action.py
│   ├── memory.py
│   ├── governance.py
│   ├── collaboration.py
│   ├── orchestrator.py
│   └── cli.py
└── patterns/                       ← 全部 28 个设计模式汇总
    └── (含前所有章节的模式文件)
```

---

## 五、Argus 进化史（核心主线）

```
ch02  argus/core.py
      └─ 会做：接收 diff → 调用 Claude → 返回审查结果（单次 PRA）

ch03  argus/perception.py  加入↓
      └─ 会做：从代码库里挑选最相关的文件作为上下文

ch04  argus/memory.py      加入↓
      └─ 会做：记住这个项目之前的审查历史，避免重复问题

ch05  argus/reasoning.py   加入↓
      └─ 会做：用 Chain of Thought 分步分析，判断问题严重程度

ch06  argus/action.py      加入↓
      └─ 会做：主动调用工具（如运行 lint），而不只是分析文字

ch07  argus/reflection.py  加入↓
      └─ 会做：对自己的审查结果再做批判，不满意就重来

ch08  argus/collaboration.py  加入↓
      └─ 会做：复杂 diff 拆给多个子 Agent 并行处理

ch09  argus/governance.py  加入↓
      └─ 会做：每个操作都做权限检查，全程记录审计日志
```

---

## 六、学习路线建议

### 第一步：建立直觉（不需要 API Key）
```bash
# 对比传统软件和 Agent
python ch01-paradigm-shift/simple_agent.py

# 感受感知模式（纯 Python）
cd ch03-perception && python patterns/context_triage.py

# 感受记忆模式（纯 Python）
cd ch04-memory && python patterns/progress_tracker.py
```

### 第二步：跑第一个真实 Agent（需要 API Key）
```bash
# 配置 Key
cp .env.example .env
# 编辑 .env，填入 ANTHROPIC_API_KEY=sk-ant-...

# 跑 Argus 核心（检测 SQL 注入）
cd ch02-architecture && python argus/core.py
```

### 第三步：按章节逐步学习
每章建议顺序：
1. 先读章节 `README.md`，了解本章目标
2. 逐个读 `patterns/` 里的文件，理解每个独立模式
3. 最后读 `argus/` 里的文件，看模式怎么组合进真实 Agent

---

## 七、快速检索：这个模式在哪里？

| 你想了解 | 去哪里找 |
|---------|---------|
| Agent 最基本的结构 | `ch02-architecture/argus/core.py` |
| 怎么过滤信息（上下文太多） | `ch03-perception/patterns/context_triage.py` |
| 怎么压缩信息 | `ch03-perception/patterns/semantic_compaction.py` |
| Agent 怎么记住历史 | `ch04-memory/patterns/hierarchical_memory.py` |
| RAG 怎么实现 | `ch04-memory/patterns/rag_pipeline.py` |
| 任务崩溃怎么恢复 | `ch04-memory/patterns/progress_tracker.py` |
| Chain of Thought 怎么写 | `ch05-reasoning/patterns/chain_of_thought.py` |
| 怎么根据难度选模型 | `ch05-reasoning/patterns/complexity_routing.py` |
| 多步骤任务怎么流水线 | `ch06-action/patterns/prompt_chain.py` |
| 工具怎么管理和路由 | `ch06-action/patterns/tool_dispatch.py` |
| 先规划再执行 | `ch06-action/patterns/plan_and_execute.py` |
| 安全三层检查 | `ch06-action/patterns/guardrail_sandwich.py` |
| 让 Agent 自我批评 | `ch07-reflection/patterns/generator_critic.py` |
| 代码修复失败自动重试 | `ch07-reflection/patterns/self_heal_loop.py` |
| 从历史经验学习 | `ch07-reflection/patterns/experience_replay.py` |
| 权限控制 | `ch09-governance/patterns/permission_gate.py` |
| 防篡改审计日志 | `ch09-governance/patterns/audit_log.py` |
| OpenAI SDK 怎么写 Agent | `ch02-architecture/demos/openai_argus.py` |
| LangGraph 怎么写 Agent | `ch02-architecture/demos/langgraph_argus.py` |

---

## 八、关键术语速查

| 术语 | 解释 |
|------|------|
| PRA 循环 | Perception-Reasoning-Action，Agent 的基本执行模型 |
| Argus | 贯穿全书的示例 Agent，功能是代码审查 |
| patterns/ | 每章的独立设计模式文件，互不依赖 |
| argus/ | 每章的 Argus 累积快照，集成了当前章和之前所有章的能力 |
| Runtime VM | Agent 的基础设施层（沙箱、状态、工具、监控） |
| Guardrail（护栏） | 对 Agent 输入/输出的安全检查规则 |
| MCP | Model Context Protocol，Anthropic 定义的工具连接协议 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| Chain of Thought | 让 LLM 分步推理，而不是直接给答案 |
| Guardrail Sandwich | 输入检查 + 执行监控 + 输出过滤三层安全 |
| Generator-Critic | 一个 Agent 生成，另一个批评，迭代改进 |
| Trust Level | 信任等级，决定 Agent 有哪些操作权限 |
| Audit Log | 防篡改的操作审计日志，用哈希链保证完整性 |
| LangGraph | 用图结构定义 Agent 流程的框架 |

---

*最后更新：随学习进度逐步完善，每章学完可在对应章节补充笔记。*
